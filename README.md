# Vision Transformer — Fine-Grained Visual Recognition
Vision Transformer fine-tuned for fine-grained image classification.
### ■ Research Highlights
- **Patch Embedding**: Splits images into $16 \times 16$ non-overlapping patches, linearly projects them, and- **Multi-Head Self-Attention**: Applies $h$ parallel attention heads to model long-range spatial dependencie- **Algorithms Focus**: Compares ViT-B/16 against ResNet-50 and EfficientNet on CUB-200, analysing how attent**Tech Stack:** Python, `PyTorch`, `Transformers`.
